<%= provide(:title, @user.name) %>
<div class="center">
  <div><%= link_to gravatar_for(@user), @user %></div>
  <h4><%= @user.name %></h4>
  <div><%= link_to "Delete", admin_user_path(@user), method: :delete, 
    confirm: "Are you sure" %></div>
</div>