<%= provide(:title, "All Brands") %>
<h1>Brands</h1>

<ul class="brands">
  <% @brands.each do |brand| %>
    <li>
      <%= link_to image_tag(brand.image.url(:thumb)), brand %>
      <div>
        <%= link_to brand.name, brand %>
      </div>
    </li>
  <% end %>
</ul>