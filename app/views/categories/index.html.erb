<%= provide(:title, "All Categories") %>
<h1>Categories</h1>

<ul class="categories">
  <% @categories.each do |category| %>
    <li>
      <%= link_to category.name, category %>
    </li>
  <% end %>
</ul>